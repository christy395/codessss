'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');const r=useRouter();async function submit(x:FormEvent){x.preventDefault();setE('');const q=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password:p})});const d=await q.json();if(!q.ok)return setE(d.error||'Login failed');r.push('/dashboard');r.refresh()}return <div className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-7"><h1 className="text-3xl font-bold">Welcome back</h1><p className="mt-2 text-zinc-500">Sign in to your BotVault workspace.</p>{e&&<p className="mt-4 rounded-lg bg-red-500/10 p-3 text-sm text-red-300">{e}</p>}<input required type="email" placeholder="Email" value={email} onChange={x=>setEmail(x.target.value)} className="mt-6 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><input required type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} className="mt-3 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><button className="mt-5 w-full rounded-xl bg-white px-4 py-3 font-semibold text-black">Sign in</button><p className="mt-5 text-center text-sm text-zinc-500">No account? <Link className="text-cyan-300" href="/register">Create one</Link></p></form></div>}
