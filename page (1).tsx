import {redirect} from 'next/navigation'; import Link from 'next/link'; import {getUser} from '../../lib/auth'; import {db} from '../../lib/db';
export default async function Bots(){const u=await getUser();if(!u)redirect('/login');const bots=await db.bot.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}});return <div className="p-6 md:p-10"><div className="flex items-end justify-between"><div><p className="text-sm text-zinc-500">Workspace</p><h1 className="text-3xl font-bold">My Bots</h1></div><Link href="/create-bot" className="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-black">Create Bot</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2">{bots.map(b=><Link href={'/bots/'+b.id} className="glass rounded-2xl p-6" key={b.id}><div className="flex justify-between"><div><h2 className="font-semibold">{b.name}</h2><p className="mt-1 text-xs text-zinc-500">{b.runtime} · {b.id}</p></div><span className="rounded-full border border-white/10 px-2.5 py-1 text-xs">{b.status}</span></div><div className="mt-7 grid grid-cols-3 gap-4 text-sm"><div><p className="text-zinc-500">Node</p><p className="mt-1 font-semibold">{b.nodeId||'—'}</p></div><div><p className="text-zinc-500">Uptime</p><p className="mt-1 font-semibold">—</p></div><div><p className="text-zinc-500">Updated</p><p className="mt-1 font-semibold">{b.updatedAt.toLocaleDateString()}</p></div></div></Link>)}</div></div>}
