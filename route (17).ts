import { NextResponse } from 'next/server'; import bcrypt from 'bcryptjs'; import { z } from 'zod'; import { db } from '../../../../lib/db'; import { createSession } from '../../../../lib/auth'; import { rateLimit } from '../../../../lib/rate-limit';
const schema=z.object({email:z.string().email(),password:z.string().min(1).max(128)});
export async function POST(req:Request){try{const ip=req.headers.get('x-forwarded-for')||'unknown';if(!rateLimit('login:'+ip,10,900000))return NextResponse.json({error:'Too many login attempts'},{status:429});const b=schema.parse(await req.json());const u=await db.user.findUnique({where:{email:b.email.toLowerCase()}});if(!u||!(await bcrypt.compare(b.password,u.passwordHash)))return NextResponse.json({error:'Invalid email or password'},{status:401});await createSession(u.id);return NextResponse.json({user:{id:u.id,email:u.email,role:u.role}});}catch{return NextResponse.json({error:'Invalid request'},{status:400});}}
