import {NextResponse} from 'next/server'; import {db} from '../../../../lib/db';
export async function POST(r:Request){if(!process.env.INTERNAL_API_TOKEN||r.headers.get('authorization')!==`Bearer ${process.env.INTERNAL_API_TOKEN}`)return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await r.json();if(!b.botId||typeof b.message!=='string')return NextResponse.json({error:'Invalid'},{status:400});await db.log.create({data:{botId:b.botId,level:b.level||'info',message:b.message.slice(0,10000)}});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Failed'},{status:500});}}
