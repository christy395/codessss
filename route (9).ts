import {NextResponse} from 'next/server'; import {db} from '../../../../../lib/db'; import {requireUser} from '../../../../../lib/auth'; export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser();const{id}=await params;if(!await db.bot.findFirst({where:{id,userId:u.id}}))return NextResponse.json({error:'Not found'},{status:404});const limit=Math.min(Number(new URL(req.url).searchParams.get('limit')||200),500);return NextResponse.json({logs:await db.log.findMany({where:{botId:id},orderBy:{createdAt:'desc'},take:limit})});}catch{return NextResponse.json({error:'Failed'},{status:500});}}
