export async function nodeRequest(path:string, init:RequestInit={}){ const base=process.env.NODE_AGENT_URL; const token=process.env.NODE_AGENT_TOKEN; if(!base||!token) throw new Error('Hosting node is not configured'); const r=await fetch(`${base}${path}`,{...init,headers:{Authorization:`Bearer ${token}`,...(init.headers||{})},cache:'no-store'}); const text=await r.text(); let body:any; try{body=JSON.parse(text)}catch{body={message:text}}; if(!r.ok) throw new Error(body.error||'Hosting node request failed'); return body; }
