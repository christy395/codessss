'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Register(){const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');const r=useRouter();async function submit(x:FormEvent){x.preventDefault();setErr('');const q=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})});const d=await q.json();if(!q.ok)return setErr(d.error||'Registration failed');r.push('/dashboard');r.refresh()}return <div className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-7"><h1 className="text-3xl font-bold">Create your account</h1>{err&&<p className="mt-4 text-sm text-red-300">{err}</p>}<input required type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} className="mt-6 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><input required minLength={8} type="password" placeholder="Password (8+ characters)" value={p} onChange={x=>setP(x.target.value)} className="mt-3 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><button className="mt-5 w-full rounded-xl bg-gradient-to-r from-violet-500 to-cyan-400 px-4 py-3 font-semibold text-black">Create account</button><p className="mt-5 text-center text-sm text-zinc-500">Already registered? <Link className="text-cyan-300" href="/login">Sign in</Link></p></form></div>}
